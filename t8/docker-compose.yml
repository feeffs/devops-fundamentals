services:
  web:
    image: nginx
    ports:
      - '8080:80'
    networks:
      - mynetwork

  db:
    image: postgres
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: db
    networks:
      - mynetwork

networks:
  mynetwork:
    driver: bridge
